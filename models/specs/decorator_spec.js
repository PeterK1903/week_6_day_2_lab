const assert = require('assert');
const Decorator = require('../decorator.js');

describe('Decorator', function()){
  beforeEach(function(){
    decorator = new Decorator()
    can = new Paint(5);
    can1 = new Paint(5);
    can2 = new Paint(5);
    can3 = new Paint(5);
    can4 = new Paint(5);
    room = new Room(20);
  });

it('should be able to add a can to stock')

};
